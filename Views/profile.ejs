<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>โปรไฟล์</title>
  <link rel="stylesheet" href="/css/profile.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
    crossorigin="anonymous" referrerpolicy="no-referrer">
  <link rel='stylesheet'
    href='https://cdn-uicons.flaticon.com/2.1.0/uicons-regular-rounded/css/uicons-regular-rounded.css'>
</head>

<body>
  <%- include('components/navbar.ejs') -%>
    <div class="container-fluid mt-3" id="main">
      <div class="row d-flex align-items-center">
        <div class="col" style="display: flex;justify-content: space-between;align-items: center;">
          <button type="button" onclick="toggleNav()" class="toggler mb-2 p-0 m-0" style="font-size: 20px">
            <i class="fi fi-rr-indent" id="toggleIcon" style="color: white"></i>
          </button>
          <nav style="--bs-breadcrumb-divider: '>'; margin-right: 67%;" aria-label="breadcrumb">
            <ol class="breadcrumb">
              <li class="breadcrumb-item"><a href="/">หน้าหลัก</a></li>
              <li class="breadcrumb-item active" aria-current="page">โปรไฟล์</li>
            </ol>
          </nav>
        </div>
        <%- include('components/userinfo.ejs') -%>
          <div class="row">
            <div class="col-12">
              <div class="card" style="box-shadow: 0px 8px 24px 0px rgba(0, 0, 0, 0.25)">
                <div class="card-body  text-center">
                  <img src="../image/Mae-Fah-Luang-University-2.png" alt="Admin" class="rounded-circle" width="150">
                  <div class="mt-3">
                    <h4>
                      <%= user.firstname %>
                        <%= user.lastname %>
                    </h4>
                    <p class="text-secondary mb-1">
                      <%= user.numberID %>
                    </p>
                    <p class="text-secondary mb-1">
                      <%=(user.role==="approver" ? 'ผู้อนุมัติ' : (user.role==="admin" ? 'คนจัดรถ' : 'พนักงาน' ))%>
                    </p>
                    <p class="text-muted font-size-sm">
                      <%= user.organization %>
                    </p>
                    <div class="d-grid gap-2">
                      <a class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#reset">เปลี่ยนรหัสผ่าน</a>
                      <div class="modal fade" id="reset" aria-hidden="true">
                        <div class="modal-dialog">
                          <div class="modal-content">
                            <div class="modal-header">
                              <h5 class="modal-title">เปลี่ยนรหัสผ่าน</h5>
                              <button type="button" class="btn-close" data-bs-dismiss="modal"
                                aria-label="Close"></button>
                            </div>
                            <div class="modal-body" id="modal-body">
                              <form action="/user/update/<%=user._id%>" method="POST">
                                <div class="row mb-3">
                                  <div class="col-sm-3">
                                    <label class="form-label">รหัสผ่านเดิม</label>
                                  </div>
                                  <div class="col-sm-9">
                                    <input type="password" class="form-control" name="oldpassword" required>
                                  </div>
                                </div>
                                <div class="row mb-3">
                                  <div class="col-sm-3">
                                    <label class="form-label">รหัสผ่านใหม่</label>
                                  </div>
                                  <div class="col-sm-9 text-secondary">
                                    <input type="password" class="form-control" name="newpassword" required>
                                  </div>
                                </div>
                                <div class="row mb-3">
                                  <div class="col-sm-3">
                                    <label class="form-label">ยืนยันรหัสผ่านใหม่</label>
                                  </div>
                                  <div class="col-sm-9 text-secondary">
                                    <input type="password" class="form-control" name="newpasswordconfirm" required>
                                  </div>
                                </div>
                                <div class="row gy-2 text-center">

                                  <div class="col-md ">
                                    <button type="submit" class="btn btn-primary ">ยืนยัน</button>
                                  </div>
                                </div>
                              </form>
                            </div>
                          </div>
                        </div>
                      </div>
                      <a class="btn btn-warning " href="/profile/edit">Edit profile</a>
                      <form action="/logout" method="POST"><button class="btn btn-danger">ออกจากระบบ</button>
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
      </div>
    </div>
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        const newPasswordInput = document.querySelector('input[name="newpassword"]');
        const newPasswordConfirmInput = document.querySelector('input[name="newpasswordconfirm"]');
        const submitButton = document.querySelector('button[type="submit"]');
        const showText = document.getElementById('showText');

        newPasswordConfirmInput.addEventListener('input', function () {
          const newPassword = newPasswordInput.value;
          const newPasswordConfirm = newPasswordConfirmInput.value;

          if (newPassword !== newPasswordConfirm) {
            newPasswordConfirmInput.setCustomValidity('รหัสผ่านใหม่และการยืนยันรหัสผ่านไม่ตรงกัน');
            showText.textContent = 'รหัสผ่านใหม่และการยืนยันรหัสผ่านไม่ตรงกัน';
          } else {
            newPasswordConfirmInput.setCustomValidity('');
            showText.textContent = '';
          }
        });

        // Optional: Disable submit button if passwords don't match
        submitButton.addEventListener('click', function (event) {
          if (newPasswordInput.value !== newPasswordConfirmInput.value) {
            event.preventDefault();

            showText.textContent = 'รหัสผ่านใหม่และการยืนยันรหัสผ่านไม่ตรงกัน';
          }
        });
      });
    </script>


    <script src="../JS/navbar.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>